import 'package:flutter/material.dart';

class DonorConsentsScreen extends StatelessWidget {
  const DonorConsentsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Donor consents')));
  }
}
