ENVIRONMENT=development

# NOT used yet - real SQL Server connection pending the final ERD from the Database Developer.
DATABASE_URL=mssql+pyodbc://user:password@server/database?driver=ODBC+Driver+18+for+SQL+Server

# PROVISIONAL auth secret - generate a real one and store it in a secret manager before any deployment.
SECRET_KEY=change-me-generate-a-real-secret
ACCESS_TOKEN_EXPIRE_MINUTES=60
